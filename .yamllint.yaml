# GitHub workflow syntax breaks two yamllint defaults that are not worth fighting:
# `on:` is a YAML 1.1 boolean, and workflow expressions do not wrap.
extends: default

rules:
  truthy:
    allowed-values: ["true", "false"]
    check-keys: false
  line-length:
    max: 120
    allow-non-breakable-words: true
  comments:
    min-spaces-from-content: 1
  # `{ type: string }` is the idiomatic compact form in an OpenAPI document and
  # contracts/openapi.yaml uses it throughout. Allowing the space is cheaper than
  # reformatting a hand-written contract to satisfy a cosmetic rule.
  braces:
    min-spaces-inside: 0
    max-spaces-inside: 1
  document-start: disable
  # A workflow's `jobs:` block is conventionally indented under its parent; the
  # default requires consistency, not a particular style.
  indentation:
    spaces: consistent
    indent-sequences: consistent

ignore: |
  .bin/
  internal/web/static/
